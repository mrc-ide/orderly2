# Deprecated functions in orderly. These functions still work (*for now*) but will be removed shortly. Please update your code.

- `orderly_location_pull_packet`; please use
  [`orderly_location_pull()`](https://mrc-ide.github.io/orderly/reference/orderly_location_pull.md)

- `orderly_location_pull_metadata`; please use
  [`orderly_location_fetch_metadata()`](https://mrc-ide.github.io/orderly/reference/orderly_location_fetch_metadata.md)

## Usage

``` r
orderly_location_pull_metadata(...)

orderly_location_pull_packet(...)
```

## Arguments

- ...:

  Arguments forwarded to the new version

## Value

See the linked pages above for the current return type.
